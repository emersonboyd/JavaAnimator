package cs3500.animator.controller;

/**
 * Represents the three states that an animation can be in, not started, in progress, and paused.
 * The states are mutually exclusive.
 */
public enum AnimationState {
  NOT_STARTED, IN_PROGRESS, PAUSED
}
